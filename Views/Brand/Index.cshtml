@model List<MedicineBrand>

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold"><i class="fas fa-tag me-2 text-success"></i>Medicine Brands</h2>
        <a asp-action="Create" class="btn btn-success"><i class="fas fa-plus me-2"></i>Add Brand</a>
    </div>

    <div class="card border-0 shadow-sm">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead class="table-success">
                        <tr><th>Logo</th><th>Name</th><th>Description</th><th>Medicines</th><th>Status</th><th>Actions</th></tr>
                    </thead>
                    <tbody>
                        @foreach (var brand in Model)
                        {
                            <tr>
                                <td>
                                    @if (brand.Logo != null)
                                    {
                                        <img src="@brand.Logo" alt="@brand.Name" style="height: 30px;" />
                                    }
                                    else
                                    {
                                        <i class="fas fa-tag text-muted"></i>
                                    }
                                </td>
                                <td class="fw-bold">@brand.Name</td>
                                <td>@brand.Description</td>
                                <td><span class="badge bg-info">@brand.Medicines.Count</span></td>
                                <td>
                                    @if (brand.IsActive)
                                    {
                                        <span class="badge bg-success">Active</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger">Inactive</span>
                                    }
                                </td>
                                <td>
                                    <a asp-action="Edit" asp-route-id="@brand.Id" class="btn btn-sm btn-outline-primary me-1"><i class="fas fa-edit"></i></a>
                                    <form asp-action="Delete" asp-route-id="@brand.Id" method="post" class="d-inline">
                                        @Html.AntiForgeryToken()
                                        <button type="submit" class="btn btn-sm btn-outline-danger" onclick="return confirm('Are you sure?')"><i class="fas fa-trash"></i></button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>